Objective - This project aims at parsing the given json input in file testInput.json to output the flattened json object.

Input - This could be any file, one example file is present in test folder called testInput.json.

Execution steps - There is a jsonUtil.sh provided along. The bash script executes the required steps for the user to
create the class file, and run the provided input as follows:

Ensure that your current directory when executing the bash file is: "projects"

***************************************************************

cat test/testInput.json | ./jsonUtil.sh

***************************************************************
